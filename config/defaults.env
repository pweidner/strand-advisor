# strand-advisor defaults (override via environment or CLI flags)

# Shared assets on the cluster (recommended)
ASSETS_DIR=${ASSETS_DIR:-/data/cephfs-1/work/groups/sanders/strand-advisor-assets}

# Reference (required)
REF_FASTA=${REF_FASTA:-$ASSETS_DIR/refs/GRCh38_full_analysis_set_plus_decoy_hla.fa}

# Optional defaults
CHROMS_FILE=${CHROMS_FILE:-$ROOT/config/chroms.grch38.txt}
COVERAGE=${COVERAGE:-0.04}
NOISE=${NOISE:-5.0}
THREADS=${THREADS:-16}

# SNP sample for HP tagging
SNP_SAMPLE=${SNP_SAMPLE:-HG00732}
